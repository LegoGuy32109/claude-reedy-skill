{
  "hooks": {
    "SessionStart": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "python3 \"${CLAUDE_PLUGIN_ROOT}/reedy-serve\" --write-runtime \"${CLAUDE_PLUGIN_ROOT}\" 2>/dev/null || python \"${CLAUDE_PLUGIN_ROOT}/reedy-serve\" --write-runtime \"${CLAUDE_PLUGIN_ROOT}\" 2>/dev/null || true"
          }
        ]
      }
    ]
  }
}
